# bookclub-edit 相依套件清單
#
# 版本都是實測跑得動的組合（在模擬 Intel Mac 的環境測過，見
# spikes/phase0/）。兩種晶片（Intel／Apple Silicon）用同一份，不要因為
# Apple Silicon 裝得到更新版本就手滑升級——夥伴的 Intel Mac 裝不起來。
# 版本上限另外寫在 constraints.txt，安裝時兩份一起帶入。
#
# 安裝方式（install.sh 會自動做，這裡記錄給人看）：
#   uv pip install --python .venv/bin/python -r requirements.txt \
#       -c constraints.txt --build-constraint build-constraints.txt

# ── 逐字對位（把逐字稿對回聲音的正確時間）──────────────────
# 主套件 qwen-asr 另外用 --no-deps 裝（見 install.sh），因為它預設會拉
# gradio、flask 這些介面用的大套件，我們用不到。這裡只裝它實際需要的部分。
transformers==4.57.6
accelerate==1.12.0
nagisa==0.2.11          # 日文斷詞（多語言對位工具內部會用到）
soynlp==0.0.493         # 韓文斷詞（同上）

# ── 分辨誰在說話（抓出老師與學員各自講了哪一段、有沒有搶話）──
pyannote.audio==3.4.0

# ── 轉文字與分析（讀書會剪輯工具第 1 步：轉文字、找老師、找重疊、找名字）──
groq==1.7.0                        # Groq API（whisper-large-v3 轉文字）
silero-vad==6.2.2                  # 轉文字前先去靜音，切段更準、省 API 額度
opencc-python-reimplemented==0.1.7 # 簡轉繁（Groq 偶爾吐簡體字）
pypinyin==0.55.0                   # 找名字：漢字轉拼音做讀音比對

# ── 聲音生成（CosyVoice：老師原音續講、學員換成匿名聲線）──────
# CosyVoice 不是 pip 套件，原始碼用 git clone 放在 third_party/CosyVoice
# （install.sh 處理）；下面是它實際會 import 到、且我們裝得起來的相依。
conformer==0.3.2
diffusers==0.29.0
HyperPyYAML==1.2.3
hydra-core==1.3.2
inflect==7.3.1
omegaconf==2.3.0
openai-whisper==20231117   # 要從原始碼建置，見 build-constraints.txt
tiktoken                   # openai-whisper 的斷詞工具
regex                      # 同上
x-transformers==2.11.24
wetext==0.0.4              # 文字正規化（數字唸法等），資源從 modelscope 下載
modelscope
lightning==2.2.4
einops
matplotlib
gdown==5.1.0
wget==3.2
pyarrow

# ── 共用（三個模型都會用到，或是環境本身需要）──────────────
torch==2.2.2
torchaudio==2.2.2
numpy==1.26.4
librosa
soundfile
huggingface_hub            # 版本由 transformers 限制在 1.0 以下，不用另外鎖
onnxruntime                # CosyVoice 的聲紋、語音 token 模型是 onnx 格式
protobuf
setuptools<81              # lightning 2.2.4 執行期需要舊版 setuptools 帶的 pkg_resources
